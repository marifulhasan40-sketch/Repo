workflows:
  android-workflow:
    name: Build Android APK
    environment:
      java: 17
      android_sdk: 34
    scripts:
      - name: Build APK
        script: |
          ./gradlew assembleRelease
    artifacts:
      - app/build/outputs/**/*.apk
